#include <stdio.h>
#define MAX 255

int main(void) {
    char string[MAX], word[MAX], minWord[MAX], maxWord[MAX];
    printf("Enter a string: ");
    scanf("%s", string);

    
    do {

    } while ()
}